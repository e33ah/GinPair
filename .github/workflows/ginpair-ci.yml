# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_ginpair-ci --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: ginpair-ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    name: ginpair-ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
  
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
       
      - name: Ensure build scripts are executable
        run: chmod +x build.cmd build.sh build.ps1

      - name: Run ArrangeStep
        run: ./build.cmd ArrangeStep

      - name: Run ConstructStep
        run: ./build.cmd ConstructStep --skip ArrangeStep

      - name: Run ExamineStep
        run: ./build.cmd ExamineStep --skip ConstructStep
